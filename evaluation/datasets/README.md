# 输入数据目录

这里只存放评测输入，不存放运行结果、日志、审计包或缓存。两个 mini 都是从同目录
标准数据集机械截取的原格式样本，不使用 Mock，也不更换测评链路。

- `locomo/`：LoCoMo 原始数据和可提交的微型样本。
- `longmemeval/`：LongMemEval 原始数据和可提交的微型样本。
完整数据文件默认被 `.gitignore` 排除，微型样本作为本地冒烟输入保留。
