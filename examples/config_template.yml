# agent-memory 装配配置示例（被 examples/quickstart.py 通过 Config.from_yaml 解析）
#
# 配置是**两级命名空间**：顶层 = 各组件（Producer 的 TOP_NAME），其下 = 具名实例（target/params）。
# 用户配置会**合并覆盖**到内置默认（纯内存离线栈）之上——只需写要改动的部分。
# 依赖写法：「引用名(字符串)」= 共享同一具名实例；「内联(映射)」= 就地建匿名实例。
#
# 下面除 globals 外全部注释掉 → 本文件等价于纯默认装配；按需取消注释即可覆盖。

# ---- 跨切面参数（写一处，各组件经 globals 读到；具名实例 params 可覆盖）----
globals:
  vector_enabled: true        # 向量索引 + 向量召回路
  graph_enabled: true         # 图召回路
  rerank_enabled: true        # 披露前精排
  embedder_dim: 64
  chunk_size: 120
  policies:                   # 运行时可变策略（交 PolicyManager；值保持字符串）
    rerank.enabled: "true"

# ---- 换存储后端：覆盖各命名空间下的 default 实例（三方后端必填参数在 build 期即校验）----
# kv_store:
#   default: { target: redis, params: { url: "redis://localhost:6379/0", db: 0 } }
# vector_store:
#   default: { target: milvus, params: { uri: "http://localhost:19530" } }

# ---- 覆盖算子参数 ----
# fuser:
#   default: { target: rrf, params: { k: 80 } }

# ---- 本地高级精排（需安装 extra: embed，并准备模型文件）----
# reranker:
#   default: { target: bge_reranker, params: { reranker_bge_model: BAAI/bge-reranker-v2-m3 } }

# ---- 跨实例分布式锁（横切原语，当前无消费方，配了也只在有人 dep 时才实例化）----
# 不设默认实现：要用就必须显式配，避免「忘了配 Redis」静默退化成不跨实例的单机锁。
# memory 实现只供单测与本地开发，不提供跨实例互斥；生产一律用 redis。
# ssl_verify 只管是否校验服务端证书，加密开关在 url 的 scheme 上（须 rediss://）。
# lock:
#   default:
#     target: redis
#     params:
#       url: "redis://localhost:6379/1"   # 与 kv_store 分库，便于单独观测与清理
#       lease_ms: 30000                   # 租约；guard 以 lease_ms/3 为周期自动续期
#       wait_timeout_ms: 10000            # 有界等待上限，超时抛 LockTimeoutError

# ---- 多实例 + 引用共享示例：构建侧与召回侧共享同一具名 vector_store ----
# vector_store:
#   shared_vec: { target: milvus, params: { uri: "http://localhost:19530" } }
# constructor:
#   default: { target: hybrid, params: { vector_store: shared_vec } }   # 引用 → 与召回侧共享
# recaller:
#   vector: { target: vector, params: { vector_store: shared_vec } }

# ---- 动态 prompt 四步编排（DynamicEvolver：extract→consolidate→reflect→落盘）----
# prompts 段（顶层，与 globals 同级）：按 phase → key → prompt 文本。
# metadata 只写 prompt 的 **key**（引用此处的命名 prompt），三步统一，运行时由
# PromptRegistry 按 phase+key 查真实文本发给 LLM。
# prompts:
#   extract:
#     episodic: "抽取事件，输出 JSON [{source_id, content, tier, tags}]"
#     preference: "抽取偏好，输出 JSON [{source_id, content, tier, tags}]"
#   consolidate:
#     episodic: "事件变化时替换旧记忆，输出 JSON {decision, existing_id, reason}"
#   reflect:
#     default: "对候选记忆做一致性反思，必要时修正 content"

# 启用动态四步：把默认 evolver 的 target 改为 dynamic（与 orchestrating 平级）。
# 可选 target：
#   - orchestrating  默认。legacy 四模式（EXTRACT/CONSOLIDATE/ASSOCIATE/FORGET），
#                    EXTRACT 走 _dedup_batch 判定+落盘耦合的旧路径。
#   - dynamic        动态 prompt 四步编排（extract→consolidate(判定)→reflect→落盘），
#                    EXTRACT 走 DynamicEvolver；其余三模式继承 orchestrating 行为。
# 仅在要调阈值时才写下面两个可选参数。
# evolver:
#   default:
#     target: dynamic
#     # dedup_medium_similarity: 0.7    # 可选：consolidate 中段阈值（< 此值直接 ADD）
#     # dedup_high_similarity: 0.9       # 可选：consolidate 高相似阈值（≥ 此值直接 NOOP）
