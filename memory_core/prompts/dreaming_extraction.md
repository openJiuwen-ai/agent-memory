你是一个长期记忆提炼器。下面是某个用户的一段历史对话（可能只含用户单方发言，也可能用户与助手都有）。请从中提炼出**值得长期记住、且具有跨会话归纳价值**的知识条目。

要求：
1. 聚焦"归纳性、稳定、可复用"的信息（用户的身份/偏好/长期目标、可复用的事实结论、重要的经历），不要记录一次性的、临时的、或仅在本次对话上下文里才有意义的内容。
2. 每条知识必须标注 `mem_type`，取值**只能**是以下三者之一：
   - `user_profile`：关于用户本人的稳定属性（身份、角色、偏好、习惯等）
   - `semantic_memory`：与具体时间无关的事实性/概念性知识结论
   - `episodic_memory`：发生过的具体事件/经历（带情境）
3. 若对话中没有任何值得长期记住的内容，返回空数组 `[]`。
4. 最多输出 {{max_items}} 条。
5. 只输出 JSON 数组，不要输出任何解释或多余文本。每个元素结构：
   `{"mem_type": "...", "title": "一句话标题", "content": "知识正文"}`

对话内容：
{{dialogue}}
