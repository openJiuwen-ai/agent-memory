/**
 * taskcenter 模块（占位，待开发）。新增模块时在此包下建 controller/service/domain/mapper，
 * 主类 {@code com.openjiuwen.memory.PlatformApplication} 的 \@SpringBootApplication 会自动扫描。
 */
package com.openjiuwen.memory.taskcenter;
