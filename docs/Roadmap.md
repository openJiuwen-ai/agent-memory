# agent-memory Roadmap

> Agent-memory在1.0版本上进行了全量的架构升级，版本规划从 **V0.2.0** 起编号。下面按照版本记录每个版本交付的主要新特性；后续版本为规划草案，随立项与 Issue 调整，不构成实现承诺。
>
> 最近一次修订日期：2026-08-29

---

## 版本总览


| 版本         | 定位           | 状态         |
| ---------- | ------------ | ---------- |
| **V0.2.0** | 结构化记忆内核可用闭环  | 📋 规划中（基线） |
| **V0.2.1** | 管理增强+分层构建及演进 | 📋 规划中     |
| **V0.2.2** | 群体记忆构建+多模态   | 📋 规划中     |


---

## V0.2.0（规划中）

本版本构建基础记忆能力，包含从数据接入、记忆构建、记忆检索、记忆管理能力，并对外提供记忆API。其主要包括以下特性

### 1. 统一记忆接口（Memory API）

- 形态无关的核心数据面：`add` / `search` / `list` / `get` / `update` / `delete` / `evolve`
- 完整 API 以 [Memory API 规约](specs/S02-memory-api.md#数据面委托-memoryengine) 为准

### 2. 编排与生命周期（Control）

- 支持按照配置或metadata进行pipeline创建
- 支持基于Space的多租户记忆隔离

### 3. 接入层（Ingest）

- `TextSource` + `SimpleIngestor`：文本 → `MemoryUnit`（不落盘）
- `PassthroughNormalizer` 文本通路

### 4. 记忆构建与演进（Construction）

- 支持全文、向量及混合检索索引，并支持 L0/L1 内容层的可选分层索引
- 支持抽取多种记忆内容（情景、语义、程序、core），支持自定义抽取模式。
- 支持构建记忆缓存，实现快速记忆检索
- 支持基于特定条件触发对缓存记忆的抽取及长期记忆构建

### 5. 记忆检索（Retrieval）

- 支持对查询进行解析及向量化
- 支持扁平及树状Filter过滤
- 支持按配置启用基于关键词、向量、图的召回
- 支持分层召回，并支持 RRF、加权 RRF、Score Max 等融合方式

### 6. 记忆存储（Storage）

- 支持可配置的 KV / Vector / Fulltext / Graph / Fusion / FS 存储能力
- 已提供可选生产后端适配器：SQLite KV、Redis KV、Milvus、Elasticsearch、nano_graphrag、Milvus Graph Fusion、本地 FS；使用时需部署相应依赖并完成连接配置

### 7. 多形态接入面（Bootstrap）

- 支持 Python Core API 进程内嵌入
- 支持CLI形式调用

### 8. Agent 插件

- JiwenSwarm `MemoryProvider` 适配（进程内 / HTTP）
- 支持对接OpenClaw

### 9 评测框架

- 支持LoCoMo / LongMemEval 进行评测

---

## V0.2.1 （规划中）

本版本计划构建或增强以下特性：

### **记忆存储**

- 对接GaussDB

### **记忆构建**

- 支持时序分层记忆构建
- 支持记忆冲突处理算法优化
- 支持多策略融合的记忆遗忘
- 支持agent记忆经验沉淀和skill生成
- 支持基于图拓展的闲时巩固
- 支持wiki文档记忆构建

### **记忆检索**

- 支持分层召回
- 支持基于wiki文档记忆的检索

### **记忆管理**

- 支持用户鉴权及权限管理
- 支持索引数据加密


### **Agent插件**

- 支持对接ClaudeCode

---

## V0.2.2 （规划中）

本版本计划构建或增强以下特性：

### **记忆存储**

- 支持多模态记忆压缩

### **记忆构建**

- 支持基于动态文件的记忆构建
- 支持多模态对话记忆构建
- 支持共享记忆增强构建
- 支持群体记忆构建

### **记忆检索**

- 支持渐进式披露召回

### **记忆管理**

- 支持记忆可追溯管理

### **多模态接入面**

- 支持基于Skill形式的记忆调用

### **Agent插件**

- 支持对接Codex、Hermes

---

## 规划原则

1. **V0.2.0 为能力基线**：后续版本只增能力或硬化，不轻易破坏 Core API 语义；破坏性变更需升 minor 并写迁移说明。
2. **features 归档纪律不变**：影响公开接口或跨模块取舍的特性，仍按「代码 → 测试 → docs/features」三连提交。
3. **与 design 的关系**：`docs/design/` 为长期愿景；本 Roadmap 是可执行切片。design 中开放问题（architecture §17）在对应版本立项时再定案。

---

## 修订记录


| 日期         | 说明                     |
| ---------- | ---------------------- |
| 2026-08-03 | 按照当日代码及与各团队对其的情况进行初稿规划 |
| 2026-08-29 | 澄清 Core API、检索融合、索引、存储能力与进程内嵌入表述 |
