"""CLI surface for the BlinkMem / JiuwenMemory memory engine.

A protocol adapter peer to ``bootstrap/server`` (the HTTP surface): it reuses the
kernel's ``handler.dispatch`` and adds no business logic. See ``DESIGN.md``.
"""
